services:
  - type: web
    name: ai-summarizer-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn summarizer_api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0